apiVersion: v1
kind: Service
metadata:
  name: jupyter
  labels: {{- include "jupyter.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8888
      targetPort: http
  selector:
    app.kubernetes.io/name: jupyter
